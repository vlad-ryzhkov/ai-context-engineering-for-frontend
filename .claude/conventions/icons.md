# Icons Convention

<!-- Fill in after project setup -->
<!-- Source: e.g. Iconify (https://iconify.design) -->
<!-- Package: e.g. `@iconify/vue` or `@iconify/react` -->
<!-- Usage: e.g. `<Icon icon="mdi:home" />` — never use SVG files directly -->
<!-- Bundle: e.g. Use offline icons only — no CDN calls in production -->

Source: Iconify
Package: `@iconify/vue` | `@iconify/react`
Usage: `<Icon icon="mdi:home" />` — import from `@iconify/react` or `@iconify/vue`
Bundle: offline icons only (`@iconify/json` subset) — no CDN calls in production
Notes: never commit raw `.svg` files into `src/` — use Iconify icon names only
