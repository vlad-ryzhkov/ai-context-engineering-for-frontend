---
globs: "*.md"
---

# Markdown Conventions

- MD040: Every fenced code block MUST have a language tag
- MD056: Pipe `|` inside table cell MUST be escaped as `\|`
- All skill output (reports, artifacts) in English
- When calculating coverage — show formula: `N_implemented / N_total`
